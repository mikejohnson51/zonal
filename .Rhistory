stop('list elements have different spatial properites: \nTry: `lapply`',
call.=FALSE)
}
} else {
file = list(file)
}
file
w       = .find_w(file[[1]], geom, ID, w)
ID
geom
w_names = c("grid_id", "w")
ID      = names(w$weight_map)[!names(w$weight_map) %in% w_names]
lapply(1:length(file),
function(x){
single_file_execute(file[[x]], w, w_name, FUN, rcl, ID)
}
)
file[[x]]
file = file[[1]]
name(file)
names(file)
file
names    = names(file)
names
file = dap
names    = names(file)
names
single_file_execute = function(file, w, w_name, name,  FUN, rcl, ID){
dt   = .zonal_io(file, w)
cols = names(dt)[!names(dt) %in% c(ID, w_names)]
if(FUN == "freq"){
dt[, frac_total := (w / sum(w, na.rm = TRUE)), by = c(ID)]
dt = dt[, .(freq = sum(frac_total, na.rm = TRUE)), by = c(ID, cols)]
if(!is.null(rcl)){
dt[[cols]] = rcl$to[match(dt[[cols]], rcl$from)]
}
exe = setnames(dt, c(ID, "value", "percentage"))
} else {
if(FUN == 'mean'){
FUN = function(x, w) { sum((x * w), na.rm = TRUE)  / sum(w, na.rm = TRUE)}
} else if(FUN == "max"){
FUN = function(x, w) { suppressWarnings(max(x, na.rm = TRUE)) }
} else if(FUN == "min"){
FUN = function(x, w) { suppressWarnings(min(x, na.rm = TRUE)) }
} else if(FUN == "mode"){
FUN = function(x, w) { suppressWarnings(getmode(x)) }
} else if(FUN == "gm_mean"){
FUN = function(x, w) { suppressWarnings(gm_mean(x)) }
} else {
stop("FUN not valid...")
}
exe = dt[, lapply(.SD, FUN = FUN, w = w), keyby = eval(ID), .SDcols = cols]
exe = setnames(dt, c(ID, name))
}
exe
}
dt   = .zonal_io(file, w)
single_file_execute = function(file, w, w_name, name,  FUN, rcl, ID){
dt   = .zonal_io(file, w)
cols = names(dt)[!names(dt) %in% c(ID, w_names)]
if(FUN == "freq"){
dt[, frac_total := (w / sum(w, na.rm = TRUE)), by = c(ID)]
dt = dt[, .(freq = sum(frac_total, na.rm = TRUE)), by = c(ID, cols)]
if(!is.null(rcl)){
dt[[cols]] = rcl$to[match(dt[[cols]], rcl$from)]
}
exe = setnames(dt, c(ID, "value", "percentage"))
} else {
if(FUN == 'mean'){
FUN = function(x, w) { sum((x * w), na.rm = TRUE)  / sum(w, na.rm = TRUE)}
} else if(FUN == "max"){
FUN = function(x, w) { suppressWarnings(max(x, na.rm = TRUE)) }
} else if(FUN == "min"){
FUN = function(x, w) { suppressWarnings(min(x, na.rm = TRUE)) }
} else if(FUN == "mode"){
FUN = function(x, w) { suppressWarnings(getmode(x)) }
} else if(FUN == "gm_mean"){
FUN = function(x, w) { suppressWarnings(gm_mean(x)) }
} else {
stop("FUN not valid...")
}
exe = dt[, lapply(.SD, FUN = FUN, w = w), keyby = eval(ID), .SDcols = cols]
exe = setnames(dt, c(ID, name))
}
exe
}
out = lapply(1:length(file),
function(x){
single_file_execute(file[[x]], w, w_name, names[x], FUN, rcl, ID)
}
)
names
file
file = file[[1]]
dt   = .zonal_io(file, w)
cols = names(dt)[!names(dt) %in% c(ID, w_names)]
if(FUN == "freq"){
dt[, frac_total := (w / sum(w, na.rm = TRUE)), by = c(ID)]
dt = dt[, .(freq = sum(frac_total, na.rm = TRUE)), by = c(ID, cols)]
if(!is.null(rcl)){
dt[[cols]] = rcl$to[match(dt[[cols]], rcl$from)]
}
exe = setnames(dt, c(ID, "value", "percentage"))
} else {
if(FUN == 'mean'){
FUN = function(x, w) { sum((x * w), na.rm = TRUE)  / sum(w, na.rm = TRUE)}
} else if(FUN == "max"){
FUN = function(x, w) { suppressWarnings(max(x, na.rm = TRUE)) }
} else if(FUN == "min"){
FUN = function(x, w) { suppressWarnings(min(x, na.rm = TRUE)) }
} else if(FUN == "mode"){
FUN = function(x, w) { suppressWarnings(getmode(x)) }
} else if(FUN == "gm_mean"){
FUN = function(x, w) { suppressWarnings(gm_mean(x)) }
} else {
stop("FUN not valid...")
}
exe = dt[, lapply(.SD, FUN = FUN, w = w), keyby = eval(ID), .SDcols = cols]
exe = setnames(dt, c(ID, name))
}
name
name = names[x]
name
name = names[1]
name
exe = setnames(dt, c(ID, name))
dt
if(FUN == 'mean'){
FUN = function(x, w) { sum((x * w), na.rm = TRUE)  / sum(w, na.rm = TRUE)}
} else if(FUN == "max"){
FUN = function(x, w) { suppressWarnings(max(x, na.rm = TRUE)) }
} else if(FUN == "min"){
FUN = function(x, w) { suppressWarnings(min(x, na.rm = TRUE)) }
} else if(FUN == "mode"){
FUN = function(x, w) { suppressWarnings(getmode(x)) }
} else if(FUN == "gm_mean"){
FUN = function(x, w) { suppressWarnings(gm_mean(x)) }
} else {
stop("FUN not valid...")
}
exe = dt[, lapply(.SD, FUN = FUN, w = w), keyby = eval(ID), .SDcols = cols]
exe = setnames(exe, c(ID, name))
exe
single_file_execute = function(file, w, w_name, name,  FUN, rcl, ID){
dt   = .zonal_io(file, w)
cols = names(dt)[!names(dt) %in% c(ID, w_names)]
if(FUN == "freq"){
dt[, frac_total := (w / sum(w, na.rm = TRUE)), by = c(ID)]
dt = dt[, .(freq = sum(frac_total, na.rm = TRUE)), by = c(ID, cols)]
if(!is.null(rcl)){
dt[[cols]] = rcl$to[match(dt[[cols]], rcl$from)]
}
exe = setnames(dt, c(ID, "value", "percentage"))
} else {
if(FUN == 'mean'){
FUN = function(x, w) { sum((x * w), na.rm = TRUE)  / sum(w, na.rm = TRUE)}
} else if(FUN == "max"){
FUN = function(x, w) { suppressWarnings(max(x, na.rm = TRUE)) }
} else if(FUN == "min"){
FUN = function(x, w) { suppressWarnings(min(x, na.rm = TRUE)) }
} else if(FUN == "mode"){
FUN = function(x, w) { suppressWarnings(getmode(x)) }
} else if(FUN == "gm_mean"){
FUN = function(x, w) { suppressWarnings(gm_mean(x)) }
} else {
stop("FUN not valid...")
}
exe = dt[, lapply(.SD, FUN = FUN, w = w), keyby = eval(ID), .SDcols = cols]
exe = setnames(exe, c(ID, name))
}
exe
}
out = lapply(1:length(file),
function(x){
single_file_execute(file[[x]], w, w_name, names[x], FUN, rcl, ID)
}
)
file = dat
file = dap
out = lapply(1:length(file),
function(x){
single_file_execute(file[[x]], w, w_name, names[x], FUN, rcl, ID)
}
)
FUN
FUN = "mean"
out = lapply(1:length(file),
function(x){
single_file_execute(file[[x]], w, w_name, names[x], FUN, rcl, ID)
}
)
out
res <- Reduce(function(...) merge(..., all = TRUE, by="ID"), out)
out
res <- Reduce(function(...) merge(..., all = TRUE, by="ID"), out)
res <- Reduce(function(...) merge(..., all = TRUE, by=ID), out)
res
devtools::load_all(".")
rm(list = c("single_file_execute"))
devtools::load_all(".")
execute_zonal(file = dap, AOI, "fip_code")
file = dap
. <- .SD <- frac_total <- NULL
if(join & is.null(geom)){ join = FALSE }
if(inherits(file, 'list')){
#are they all the same?
if(any(length(unique(unlist(lapply(file, nrow)))) > 1,
length(unique(unlist(lapply(file, ncol)))) > 1,
length(unique(unlist(lapply(file, ncell)))) > 1,
length(unique(unlist(lapply(file, xmin)))) > 1,
length(unique(unlist(lapply(file, ymin)))) > 1)){
stop('list elements have different spatial properites: \nTry: `lapply`',
call.=FALSE)
}
} else {
file = list(file)
}
w       = .find_w(file[[1]], geom, ID, w)
w_names = c("grid_id", "w")
ID      = names(w$weight_map)[!names(w$weight_map) %in% w_names]
names    = names(file)
out = lapply(1:length(file),
function(x){
single_file_execute(file[[x]], w, w_name, names[x], FUN, rcl, ID)
}
)
exe <- Reduce(function(...) merge(..., all = TRUE, by=ID), out)
merge(geom, exe, by = ID)
devtools::load_all(".")
execute_zonal(file = dap, AOI, "fip_code")
dap[[1]]
execute_zonal(file = dap[[1]], AOI, "fip_code")
file = dap[[1]]
. <- .SD <- frac_total <- NULL
if(join & is.null(geom)){ join = FALSE }
if(inherits(file, 'list')){
#are they all the same?
if(any(length(unique(unlist(lapply(file, nrow)))) > 1,
length(unique(unlist(lapply(file, ncol)))) > 1,
length(unique(unlist(lapply(file, ncell)))) > 1,
length(unique(unlist(lapply(file, xmin)))) > 1,
length(unique(unlist(lapply(file, ymin)))) > 1)){
stop('list elements have different spatial properites: \nTry: `lapply`',
call.=FALSE)
}
} else {
file = list(file)
}
file
dap[[1]]
w       = .find_w(file[[1]], geom, ID, w)
w_names = c("grid_id", "w")
ID      = names(w$weight_map)[!names(w$weight_map) %in% w_names]
names    = names(file)
names
length(file)
if(is.null(names)){rep(NA, length(file))}
if(is.null(names)){paste0('', 1:length(file))}
if(is.null(names)){names = paste0('', 1:length(file))}
names
if(is.null(names)){names = paste0('V', 1:length(file))}
out = lapply(1:length(file),
function(x){
single_file_execute(file[[x]], w, w_name, names[x], FUN, rcl, ID)
}
)
exe <- Reduce(function(...) merge(..., all = TRUE, by=ID), out)
if(join){
merge(geom, exe, by = ID)
} else {
exe
}
names
names    = names(file)
if(is.null(names)){names = paste0('V', 1:length(file))}
names
devtools::load_all(".")
execute_zonal(file = dap, AOI, "fip_code")
execute_zonal(file = dap[[1]], AOI, "fip_code")
test = execute_zonal(file = dap, AOI, "fip_code")
plot(test[names(dap)])
plot(test[names(dap)], nrow = 2
)
plot(test[names(dap)], nrow = 2)
warnings()
plot(test[names(dap)], nrows = 2)
plot(test[names(dap)])
library(opendap.catalog)
cat = search('terraclim')[1:6,]
AOI = AOI::aoi_get(state = "NC", county = "all")
system.time({
(dap <- dap(
catolog = cat,
AOI = AOI,
startDate = "2005-01-01",
))
})
test = execute_zonal(file = dap, AOI, "fip_code")
plot(test[names(dap)])
execute_zonal(file = dap[[1]], AOI, "fip_code")
system.time({
(dap <- dap(
catolog = cat[1,],
AOI = AOI,
startDate = "2005-01-01",
endDate   = "2005-05-01"
))
})
dap
test = execute_zonal(file = dap, AOI, "fip_code")
file = dap
. <- .SD <- frac_total <- NULL
if(join & is.null(geom)){ join = FALSE }
if(inherits(file, 'list')){
#are they all the same?
if(any(length(unique(unlist(lapply(file, nrow)))) > 1,
length(unique(unlist(lapply(file, ncol)))) > 1,
length(unique(unlist(lapply(file, ncell)))) > 1,
length(unique(unlist(lapply(file, xmin)))) > 1,
length(unique(unlist(lapply(file, ymin)))) > 1)){
stop('list elements have different spatial properites: \nTry: `lapply`',
call.=FALSE)
}
} else {
file = list(file)
}
w       = .find_w(file[[1]], geom, ID, w)
w_names = c("grid_id", "w")
ID      = names(w$weight_map)[!names(w$weight_map) %in% w_names]
names    = names(file)
names
if(is.null(names)){names = paste0('V', 1:length(file))}
names
dt   = .zonal_io(file, w)
cols = names(dt)[!names(dt) %in% c(ID, w_names)]
if(FUN == "freq"){
dt[, frac_total := (w / sum(w, na.rm = TRUE)), by = c(ID)]
dt = dt[, .(freq = sum(frac_total, na.rm = TRUE)), by = c(ID, cols)]
if(!is.null(rcl)){
dt[[cols]] = rcl$to[match(dt[[cols]], rcl$from)]
}
exe = setnames(dt, c(ID, "value", "percentage"))
} else {
if(FUN == 'mean'){
FUN = function(x, w) { sum((x * w), na.rm = TRUE)  / sum(w, na.rm = TRUE)}
} else if(FUN == "max"){
FUN = function(x, w) { suppressWarnings(max(x, na.rm = TRUE)) }
} else if(FUN == "min"){
FUN = function(x, w) { suppressWarnings(min(x, na.rm = TRUE)) }
} else if(FUN == "mode"){
FUN = function(x, w) { suppressWarnings(getmode(x)) }
} else if(FUN == "gm_mean"){
FUN = function(x, w) { suppressWarnings(gm_mean(x)) }
} else {
stop("FUN not valid...")
}
exe = dt[, lapply(.SD, FUN = FUN, w = w), keyby = eval(ID), .SDcols = cols]
exe = setnames(exe, c(ID, name))
}
exe
file = dap
dap
dt   = .zonal_io(file, w)
cols = names(dt)[!names(dt) %in% c(ID, w_names)]
dt   = .zonal_io(file, w)
cols = names(dt)[!names(dt) %in% c(ID, w_names)]
file
file = dap[[1]]
dt   = .zonal_io(file, w)
cols = names(dt)[!names(dt) %in% c(ID, w_names)]
cols
if(!is.null(name)){
names = gsub("V", paste0(name, "_", cols))
} else {
names = cols
}
name
cols
names = gsub("V", paste0(name, "_"), cols))
names
names = gsub("V", paste0(name, "_"), cols)
names
if(!is.null(name)){
names = gsub("V", paste0(name, "_"), cols)
} else {
names = cols
}
if(FUN == "freq"){
dt[, frac_total := (w / sum(w, na.rm = TRUE)), by = c(ID)]
dt = dt[, .(freq = sum(frac_total, na.rm = TRUE)), by = c(ID, cols)]
if(!is.null(rcl)){
dt[[cols]] = rcl$to[match(dt[[cols]], rcl$from)]
}
exe = setnames(dt, c(ID, "value", "percentage"))
} else {
if(FUN == 'mean'){
FUN = function(x, w) { sum((x * w), na.rm = TRUE)  / sum(w, na.rm = TRUE)}
} else if(FUN == "max"){
FUN = function(x, w) { suppressWarnings(max(x, na.rm = TRUE)) }
} else if(FUN == "min"){
FUN = function(x, w) { suppressWarnings(min(x, na.rm = TRUE)) }
} else if(FUN == "mode"){
FUN = function(x, w) { suppressWarnings(getmode(x)) }
} else if(FUN == "gm_mean"){
FUN = function(x, w) { suppressWarnings(gm_mean(x)) }
} else {
stop("FUN not valid...")
}
exe = dt[, lapply(.SD, FUN = FUN, w = w), keyby = eval(ID), .SDcols = cols]
exe = setnames(exe, c(ID, name))
}
exe
devtools::load_all(".")
out = lapply(1:length(file),
function(x){
single_file_execute(file[[x]], w, w_name, names[x], FUN, rcl, ID)
}
)
devtools::load_all(".")
test = execute_zonal(file = dap, AOI, "fip_code")
test
names(dap)
grepl(names(dap), names(test))
plot(test[grepl(names(dap), names(test))])
cat = search('terraclim')[1:6,]
AOI = AOI::aoi_get(state = "NC", county = "all")
system.time({
(dap <- dap(
catolog = cat,
AOI = AOI,
startDate = "2005-01-01" ))
})
AOI = AOI::aoi_get(state = "souht", county = "all")
AOI = AOI::aoi_get(state = "south", county = "all")
system.time({
(dap <- dap(
catolog = cat,
AOI = AOI,
startDate = "2005-01-01" ))
})
test = execute_zonal(file = dap, AOI, "fip_code")
plot(test[grepl(names(dap), names(test))])
names(dap)
test
names(test)
grepl(names(dap), names(test))
names(test)
names(dap)
plot(test[grepl(paste(names(dap), collapse = "|"), names(test))])
plot(test[grepl(paste(names(dap), collapse = "|"), names(test))],
border = NA)
AOI = AOI::aoi_get(state = "TX", county = "all")
system.time({
(dap <- dap(
catolog = cat,
AOI = AOI,
startDate = "2005-01-01" ))
})
test = execute_zonal(file = dap, AOI, "fip_code")
plot(test[grepl(paste(names(dap), collapse = "|"), names(test))],
border = NA)
system.time({
test = execute_zonal(file = dap, AOI, "fip_code")
})
system.time({
(dap <- dap(
catolog = cat[4,],
AOI = AOI,
startDate = "2005-01-01",
endDate   = "2005-12-01"
))
})
plot(test[grepl(paste(names(dap), collapse = "|"), names(test))],
border = NA)
system.time({
test = execute_zonal(file = dap, AOI, "fip_code")
})
plot(test[grepl(paste(names(dap), collapse = "|"), names(test))],
border = NA)
devtools::document()
devtools::load_all()
devtools::install_local(force = TRUE)
single_file_execute
devtools::load_all(".")
single_file_execute
devtools::load_all(".")
cat = search('terraclim')[1:6,]
AOI = AOI::aoi_get(state = "TX", county = "all")
library(opendap.catalog)
cat = search('terraclim')[1:6,]
AOI = AOI::aoi_get(state = "TX", county = "all")
system.time({
(dap <- dap(
catolog = cat,
AOI = AOI,
startDate = "2005-01-01" ))
})
system.time({
test = execute_zonal(file = dap, AOI, "fip_code")
})
devtools::document()
devtools::load_all(".")
system.time({
test = execute_zonal(file = dap, AOI, "fip_code")
})
devtools::load_all(".")
system.time({
test = execute_zonal(file = dap, AOI, "fip_code")
})
devtools::document()
devtools::load_all()
devtools::install_local(force = TRUE)
